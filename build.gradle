group 'if4031.pat.riak'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.basho.riak:riak-client:2.0.0'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
